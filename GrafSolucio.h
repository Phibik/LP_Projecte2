#pragma once
#include "pch.h"

#include "Util.h"
#include "MapaSolucio.h"
/*
class GrafSolucio
{
public:
	GrafSolucio(): nNodes(0), nArestes(0) {}
	GrafSolucio(const std::vector<CamiBase*>& camins);
	GrafSolucio(const std::vector<Coordinate>& nodes, const std::vector<std::vector<int>>& matriu_adj);
	GrafSolucio(const std::vector<Coordinate>& nodes, const std::vector<std::vector<int>>& parelles_nodes, const std::vector<int>& pesos);
	~GrafSolucio() {}

	int getNumNodes() const { return nNodes; }
	int getNumArestes() const { return nArestes; }

	void inserirAresta(int posNode1, int posNode2, int pes);
	void afegirNode(const Coordinate& node);
	void crearMatriu(const std::vector<std::vector<int>>& parelles, const std::vector<int>& pesos);

private:
	std::vector<Coordinate> m_nodes;
	std::vector<std::vector<int>> matriu;	// adjecència
	int nNodes;
	int nArestes;
};
*/
